import moment from "moment";

export const getDateInStandardFormat = (date_time) => {
  return moment(date_time).format("YYYY-MM-DD");
};

export const getDateInStandardFormat2 = (date_time) => {
  return moment(date_time).format("YYYY-MM-DD");
};

export const getDateInStandardForma3 = (date_time) => {
  return moment(date_time).format("YYYY-MM-DD");
};

export const getDateInStandardForma4 = (date_time) => {
  return moment(date_time).format("YYYY-MM-DD");
};

export const getDateInStandardForma5 = (date_time) => {
  return moment(date_time).format("YYYY-MM-DD");
};
